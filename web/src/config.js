/**
 * Configuration for API connection
 * Production API URL is hardcoded for Same.new deployment
 */

const config = {
  // Production Railway API URL - hardcoded for deployment
  API_BASE_URL: 'https://trustpilot-enricher-production.up.railway.app'
}

export default config
